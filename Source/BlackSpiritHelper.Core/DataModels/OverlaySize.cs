namespace BlackSpiritHelper.Core
{
    /// <summary>
    /// Overlay size
    /// Height (<see cref="OverlayOrientation.Horizontal"/>) or Width (<see cref="OverlayOrientation.Vertical"/>).
    /// </summary>
    public enum OverlaySize
    {
        /// <summary>
        /// Small. 
        /// Value is in the final size (px).
        /// </summary>
        Small = 30,

        /// <summary>
        /// Normal. 
        /// Value is in the final size (px).
        /// </summary>
        Normal = 40,

        /// <summary>
        /// Large. 
        /// Value is in the final size (px).
        /// </summary>
        Large = 60,
    }
}
