<local:BasePage x:TypeArguments="core:SchedulePageViewModel"  x:Class="BlackSpiritHelper.SchedulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BlackSpiritHelper"
      xmlns:core="clr-namespace:BlackSpiritHelper.Core;assembly=BlackSpiritHelper.Core"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SchedulePage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding ApplicationDataContent.ScheduleDesignModel.TemplatePredefinedList,
                                    Source={x:Static local:ViewModelLocator.Instance}}">

            <ItemsControl.ItemTemplate>
                <DataTemplate>

                    <ItemsControl ItemsSource="{Binding SchedulePresenter}">

                        <ItemsControl.Template>
                            <ControlTemplate>

                                <StackPanel Orientation="Vertical">
                                    <Button Content="1" Command="{Binding Btn1Command}" Width="300" Padding="10" />
                                    <ItemsPresenter />
                                </StackPanel>

                            </ControlTemplate>
                        </ItemsControl.Template>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <ItemsControl ItemsSource="{Binding TimeList}">

                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal">
                                            </StackPanel>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.Template>
                                        <ControlTemplate>

                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding DayOfWeek, Converter={local:ObjectToStringConverter}}" />
                                                <ItemsPresenter />
                                            </StackPanel>

                                        </ControlTemplate>
                                    </ItemsControl.Template>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>

                                            <ItemsControl ItemsSource="{Binding ItemListPresenter}">

                                                <ItemsControl.Template>
                                                    <ControlTemplate>

                                                        <StackPanel Orientation="Vertical" Margin="5">
                                                            <TextBlock Text="{Binding Time, Converter={local:TimeSpanToStringConverter}, ConverterParameter='hm'}" />
                                                            <ItemsPresenter />
                                                        </StackPanel>

                                                    </ControlTemplate>
                                                </ItemsControl.Template>

                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>

                                                        <TextBlock Text="{Binding Name}"
                                                                   FontFamily="{StaticResource LatoBold}"
                                                                   Foreground="{Binding ColorHEX, Converter={local:StringRGBToBrushConverter}}"/>

                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>

                                            </ItemsControl>

                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>

                                </ItemsControl>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>
    </ScrollViewer>
</local:BasePage>
